Curl works. The Connectors UI does not. Free MIT checker reads metadata but never finishes a handshake. $149 attach trace: we attempt Claude, Cursor, Desktop, and Grok against your server and write which step died — failures welcome, refund if we can't say why. Fix rounds quoted after. Not a marketplace charge.
