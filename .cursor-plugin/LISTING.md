# Cursor official Marketplace

Do **not** submit this git tree to cursor.com/marketplace/publish as-is.

Publisher Terms §3.1: the listing is free; no fees directly or indirectly for access to or use of a Plugin through the Marketplace. This repo is MIT at the root and proprietary under `template/`. Reviewers read source.

If a Cursor listing happens, it must be a **diagnose-only** view: the skill in `skills/mcp-oauth-connect/` (MIT), not a paid template upsell. Charge lives on https://gfbytes.com/products/mcp-oauth-connect/ via Payment Link, off-catalog.

This file is the listing constraint. It is not a second plugin.
